
## Configurazione di Spring/Hibernate (SessionFactory)

hibernate.dialect=org.hibernate.dialect.PostgreSQL9Dialect
hibernate.default_schema=public
hibernate.hbm2ddl.auto=create
#hibernate.hbm2ddl.auto=validate # validate the schema, makes no changes to the database.
#hibernate.hbm2ddl.auto=update # update the schema.
#hibernate.hbm2ddl.auto=create # creates the schema, destroying previous data.
#hibernate.hbm2ddl.auto=create-drop # drop the schema at the end of the session.

## Connessione al DB

jdbc.driver.class=org.postgresql.Driver
jdbc.url=jdbc:postgresql://localhost:5432/mir-commons
jdbc.user=devtest
#jdbc.password=~mirland
jdbc.password=D3vT35t
